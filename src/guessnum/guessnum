if [ $1 -gt $2 ] 2>/dev/null
then echo "$1は$2より大きい！"
elif  [ $1 -eq $2 ] 2>/dev/null
then echo "$1と$2は同じ！"
elif [ $1 -lt $2 ] 2>/dev/null
then echo "$1は$2より小さい！"
else 
    echo -e "guessnum: 2つの数字の大小を比較する\\nusage: guessnum数値数値"
fi
